import {
  brand,
  manufacturer,
  modelName,
  deviceYearClass,
  osName,
  osVersion,
} from 'expo-device';

// const {
//   brand,
//   manufacturer,
//   modelName,
//   deviceYearClass,
//   osName,
//   osVersion,
// } = Device;

const deviceInfo = {
  brand,
  manufacturer,
  modelName,
  deviceYearClass,
  osName,
  osVersion,
};

export default deviceInfo;
