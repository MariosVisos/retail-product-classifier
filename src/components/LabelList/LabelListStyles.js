import { StyleSheet } from 'react-native';
import Colors from '../../constants/Colors';

const styles = StyleSheet.create({
  footerContainer: { height: 68 },
  headerContainer: { alignItems: 'center', flexDirection: 'row', padding: 8 },
  headerText: { color: Colors.secondary, fontSize: 20 },
});

export default styles;
